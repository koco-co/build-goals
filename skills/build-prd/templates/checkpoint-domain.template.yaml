{
  "schema_version": "1.0",
  "domain_id": "{{domain-id}}",
  "name": "{{功能域名称}}",
  "status": "confirmed",
  "confirmed_at": "{{ISO-8601 时间}}",
  "dependencies": [],
  "summary": "{{用户确认的功能域范围与目标}}",
  "features": [
    {
      "id": "F-001",
      "name": "{{功能名称}}",
      "user_inputs": [
        "{{代表性用户输入}}"
      ],
      "ui_states": [
        {
          "name": "{{状态名称}}",
          "enter_when": "{{进入条件}}",
          "sketch": "{{文本界面}}",
          "transitions": [
            "{{用户操作 → 下一状态}}"
          ]
        }
      ],
      "product_state_flow": "{{触发、判断、产品状态、数据更新、失败恢复、循环和结束条件}}",
      "outputs": {
        "semantic": ["{{语义要求}}"]
      },
      "acceptance": [
        "{{Given/When/Then 验收结果}}"
      ]
    }
  ],
  "evidence": [
    "{{项目事实、用户决定或公开来源引用}}"
  ]
}
