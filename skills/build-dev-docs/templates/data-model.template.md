# 数据模型

## 实体关系

| 实体 | 业务含义 | 关系与基数 | 依据 |
| --- | --- | --- | --- |
| {{实体}} | {{引用 PRD}} | {{关联对象、外键或引用方式}} | {{有效 Schema 或实现}} |

## {{实体名称}}

Schema：{{现有 JSON Schema / 类型 / 迁移的实际路径；不存在时说明字段的代码依据。}}

ID：{{引用 GLOSSARY 的 ID 格式，并说明此实体的唯一性与生成位置。}}

| 字段 | 类型 | 必填 / 可空 | 默认值 | 约束 | 含义 | Schema / 实现定位 |
| --- | --- | --- | --- | --- | --- | --- |
| {{字段}} | {{类型}} | {{规则}} | {{值或无}} | {{范围、枚举或引用}} | {{含义}} | {{定位}} |

{{为每个核心实体重复字段字典，记录与有效 Schema 不一致的待确认项。}}
