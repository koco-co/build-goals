# 功能域架构：{{功能域名称}}

- 文档状态：{{已确认}}
- 更新时间：{{YYYY-MM-DD}}
- 功能域 ID：{{domain-id}}
- 直接依赖：{{无或功能域 ID}}

> 模板占位以 `{{…}}` 标记，交付时全部替换为真实值；「文档状态」脚本只接受「已确认」。

## 功能域边界

{{本域职责、入口和范围；只记录已确认的排除或修改边界。}}

## 需求映射

{{路线 1–3 列 F/AC/行为样例；路线 4 列 AUD Finding 和必须保持的外部行为。}}

## 组件与依赖

{{本域组件、依赖方向、直接依赖契约和共享组件所有者。}}

## 接口与数据契约

{{输入与输出，以及本域实际存在的错误、状态、数据或公开契约。}}

## 产品交互契约引用

{{仅在本域存在前端、GUI、TUI 或 CLI 时保留。引用需求包中的功能、可见状态、产品状态流和验收 ID；迁移路线引用对应 Finding。不得复制完整界面草图或产品状态机。}}

## 技术状态流

```text
{{仅在本域存在 UI 时保留。基于已引用的产品交互契约，展示触发、判断、前后端边界、持久化、异步处理、失败回滚、循环、模块调用和结束条件。}}
```

## 验证策略

{{首个验证证据和与本域验收直接对应的验证方式。}}
