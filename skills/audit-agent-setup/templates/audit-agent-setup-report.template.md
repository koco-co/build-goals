# Audit Agent Setup 报告模板

## 检查范围

- 项目：`<绝对路径>`
- 项目阶段：`<当前阶段及其证据>`
- 适用领域：`<领域>`
- 本轮排除：`<不适用领域和固定非目标>`

## 未发现问题

`<没有问题时说明检查证据和结论；删除“发现的问题”及确认段。>`

## 发现的问题

### 1. <问题标题>

- 证据：`<文件、配置、命令结果或项目事实>`
- 修复方案：`<具体修复>`
- 影响文件：`<准确路径>`
- 验证方式：`<命令或场景>`

`<按相同字段继续编号；不要加入额外分类。>`

`<用户只要求检查时：是否按以上方案修复列出的问题？>`

`<用户已明确要求检查并修复时：将按以上范围直接修复、验证并重新运行健康检查。>`

## 修复后交付

- 已修复：`<问题编号与结果>`
- 验证：`<实际命令、退出状态和关键结果>`
- 复检：`<重新运行健康检查的结论>`
- 仍未修复：`<未确认、阻塞或失败项；没有则写无>`
- 未验证：`<缺少的真实客户端或外部证据；没有则写无>`
