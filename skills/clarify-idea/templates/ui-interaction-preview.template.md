# UI 交互预览

## 状态 1：{{状态名称}}

```text
{{完整文本界面；展示信息层级、关键文案、控件及其当前状态}}
```

- 进入条件：{{从哪个状态进入，或由什么事件、判断结果触发}}
- 用户操作：{{当前可执行操作}}
- 操作结果与下一状态：{{操作 → 状态 N；逐项列出}}

{{按完整受影响流程继续枚举“状态 2、状态 3……”，不保留本说明。}}

## 状态流

```text
{{触发}}
    │
    ▼
{{判断、状态变化、数据更新、失败恢复、循环与结束条件}}
```

- 状态或数据更新：{{实际更新内容与时机}}
- 失败恢复：{{实际存在时填写；不存在时删除}}
- 结束条件：{{流程完成或停止的条件}}

## 交互契约

- 必须存在的状态：{{状态列表}}
- 信息、控件与关键文案：{{不可缺失的内容}}
- 操作与转换：{{必须保持的转换}}
- 数据与恢复：{{数据更新及失败恢复要求}}

## 文本预览的精度说明

{{说明字符边框、间距、尺寸和视觉风格只表示大概效果，不属于像素级验收。}}
