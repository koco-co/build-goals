# 可恢复检查点样例

> [!todo] 当前进度
> `OWN-01` 正在验证“预测 move 后的可用变量”。

## 场景

一个字符串先赋给新变量，随后旧变量又被打印。你不能运行代码，但可以根据所有权模型判断编译结果。

## 问题

指出哪一行会失败，并用“值的所有者发生了什么变化”解释。若你认为会通过，也要说明析构责任属于谁。

## 提示

首次展示时不提供。用户混淆复制与转移后，只提示“这个类型是否实现隐式复制”，仍不直接给结论。

## 可恢复证据

记录保存题目版本、用户原始回答、结论、证据缺口和下一步。下次恢复同一检查点，而不是凭聊天记忆重新出题。
