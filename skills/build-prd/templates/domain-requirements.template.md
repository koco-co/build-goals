# 功能域：{{功能域名称}}

- 文档状态：已确认
- 功能域 ID：{{domain-id}}
- 依赖功能域：{{无或 domain-id 列表}}

## 功能域范围

{{说明本域作用、包含内容、角色和入口；只记录已确认的排除项与对外契约。}}

## 用户能力与旅程

{{说明用户从进入本域、提供输入、接收反馈、跨域衔接到完成或恢复的旅程。}}

## 功能详细设计

> 每个功能域通常包含多项功能。每项功能复制下面的 `### F-NNN {{功能名称}}` 整节，并递增编号（F-001、F-002、F-003……），验收编号与行为样例 ID 随之递增。

### F-001 {{功能名称}}

#### 作用与目标

{{该功能解决的问题和用户可观察结果。}}

#### 适用角色、入口与前置条件

{{角色、入口、开始前状态和直接依赖。}}

#### 用户输入契约

| 输入项 | 提供者 | 必填 | 格式与范围 | 示例 |
| --- | --- | --- | --- | --- |
| {{输入名称}} | {{角色或系统}} | {{是/否}} | {{已确认的格式与范围}} | {{真实代表性输入}} |

#### 输出契约

| 输出内容 | 呈现形式 | 触发条件 | 语义要求 | 完整示例 |
| --- | --- | --- | --- | --- |
| {{输出}} | {{页面/文本/文件/数据}} | {{条件}} | {{必须覆盖的含义}} | {{完整结果}} |

#### 设计依据

{{简要说明采用的已验证事实、用户决定或公开实践。}}

#### 行为样例

- `SAMPLE-{{DOMAIN}}-001`：典型正确输入。

仅在需求或已验证产品事实中确实存在时，增加用户交互与追问、状态与提示文案、对外契约、异常与恢复、产品质量要求等章节，以及 `clarification`、`invalid`、`not_applicable` 或 `boundary` 行为样例。

#### 验收标准

- `F-001-AC-01` Given {{用户可建立的前置状态}}，When {{用户操作或可观察事件}}，Then {{唯一、具体、可检查的外部结果}}。
