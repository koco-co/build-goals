# 功能域：{{功能域名称}}

- 文档状态：已确认
- 功能域 ID：{{domain-id}}
- 依赖功能域：{{无或 domain-id 列表}}

## 功能域范围

{{说明本域作用、包含内容、明确排除、角色、入口和必须保持的对外契约。}}

## 用户能力与旅程

{{说明用户从进入本域、提供输入、接收反馈、跨域衔接到完成或恢复的旅程。}}

## 功能详细设计

### F-001 {{功能名称}}

#### 作用与目标

{{该功能解决的问题和用户可观察结果。}}

#### 适用角色、入口与前置条件

{{角色、入口、开始前状态和直接依赖。}}

#### 用户输入契约

| 输入项 | 提供者 | 必填 | 格式与范围 | 默认值 | 校验规则 | 正确示例 | 错误示例 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| {{输入名称}} | {{角色或系统}} | {{是/否}} | {{格式与边界}} | {{默认值或无}} | {{校验}} | {{真实代表性输入}} | {{无效输入}} |

#### 用户交互与追问

1. {{用户从哪里进入并做什么。}}
2. {{信息不足时产品具体追问什么。}}
3. {{产品如何反馈，用户如何继续、取消或恢复。}}

#### 状态与提示文案

| 状态 | 触发条件 | 最终文案 | 后续动作 |
| --- | --- | --- | --- |
| {{状态}} | {{条件}} | “{{可直接使用的最终文案}}” | {{下一步}} |

#### 输出契约

| 输出内容 | 呈现形式 | 触发条件 | 固定结构 | 语义要求 | 运行时可变内容 | 完整示例 |
| --- | --- | --- | --- | --- | --- | --- |
| {{输出}} | {{页面/文本/文件/数据}} | {{条件}} | {{必须完全一致的结构}} | {{必须覆盖的含义}} | {{允许变化的值及格式}} | {{完整结果}} |

#### 对外契约

{{记录用户依赖的命令、路径、字段、格式、公开 API、文件或错误行为；明确内部实现可以重新设计。}}

#### 异常、边界与恢复

{{逐项说明无效、不适用、权限、取消、失败和边界输入的反馈、输入保留与恢复。}}

#### 产品质量要求

{{只写用户可观察、可测量的响应、权限、隐私、无障碍、兼容和恢复结果。}}

#### 设计依据

{{简要说明采用的已验证事实、用户决定或公开实践。}}

#### 行为样例

- `SAMPLE-{{DOMAIN}}-001`：典型正确输入。
- `SAMPLE-{{DOMAIN}}-002`：信息不足并追问。
- `SAMPLE-{{DOMAIN}}-003`：无效或不适用输入。
- `SAMPLE-{{DOMAIN}}-004`：边界输入。

#### 验收标准

- `F-001-AC-01` Given {{用户可建立的前置状态}}，When {{用户操作或可观察事件}}，Then {{唯一、具体、可检查的外部结果}}。
