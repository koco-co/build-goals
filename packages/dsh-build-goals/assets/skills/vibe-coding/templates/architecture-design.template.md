# 架构设计方案

- 文档状态：{{已确认}}
- 更新时间：{{YYYY-MM-DD}}
- 基线 HEAD：{{commit-sha 或非 Git 说明}}
- 项目路线：{{新项目/现有项目续建}}
- 旧项目参考：{{不参考/按用户指定范围/当前项目}}
- 允许参考范围：{{具体路径、能力、契约，或 N/A（不参考旧项目）}}
- 需求快照：`docs/产品需求/需求包清单.yaml`
- 适用范围：{{项目与本次实施范围}}

> 模板占位以 `{{…}}` 标记，交付时全部替换为真实值；「文档状态」脚本只接受「已确认」。

## 需求与约束

| 功能域 | 功能/验收 | 外部行为与契约 | 架构能力 | 验证层 |
| --- | --- | --- | --- | --- |
| {{功能域}} | {{F-001 / AC}} | {{输入、输出或公开契约}} | {{组件或能力}} | {{实际验证方式}} |

{{只记录需求或项目事实支持的运行环境、团队、维护、兼容、部署和排除项。}}

## 调研与方案比较

{{与架构决策直接相关的一手来源、可行候选方案、比较、最终选择和拒绝理由。}}

## 目标架构

{{系统边界、运行时、部署拓扑、数据流、跨域依赖和依赖方向。}}

## 技术选型

| 领域 | 选择与版本策略 | 原因 | 替代方案 | 验证命令 |
| --- | --- | --- | --- | --- |
| {{领域}} | {{工具与策略}} | {{原因}} | {{替代}} | `{{command}}` |

## 目录与模块边界

```text
{{完整目标目录树，包含 docs 文档族}}
```

{{说明模块职责、允许依赖、禁止依赖、功能域所有权和共享契约所有者。}}

## 接口与数据契约

{{只说明项目实际存在的 API、CLI、UI、事件、错误、状态、数据或外部服务契约。}}

## 测试与质量策略

{{说明各项需求采用的实际验证方式；不创建没有对应行为的测试层。}}

## 交付与运行

{{记录项目实际使用的安装、开发、启动、构建、运行、部署、配套 Skill 和项目指令 readiness。需求或已验证风险涉及安全、配置或回退时，在此增加对应章节。}}

## 验收标准

- 架构覆盖需求包全部纳入功能域、功能、验收和外部契约。
- 全局方案与 `功能域/*.md` 边界一致，没有遗漏直接依赖。
- 已选择的工具链、目录、契约、验证、安全和运行方式可执行。
- 不存在未确认决策、模板占位或未经授权的旧项目参考。
