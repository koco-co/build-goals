---
name: {{skill-name}}
description: {{核心能力；允许模型调用时，还要写明适用场景、排除条件及与相邻 Skill 的区别}}
---

# Outcome

{{一句话说明这个 Skill 最终解决什么问题。}}

## Routing

- 收到 {{A 类型输入}} 时，执行 {{分支 A}}。
- 收到 {{B 类型输入}} 时，执行 {{分支 B}}。
- 不属于本 Skill 时，{{明确结束、转交或说明边界}}。

## Steps

1. 查明事实
   - 读取 {{来源}}。
   - 不向用户询问可以自行查明的事实。
   - 完成条件：{{所有必需来源均已检查}}。

2. 确认关键决策
   - 只询问无法从环境确定，并且会改变结果的决策。
   - 完成条件：{{实现范围和验收标准明确}}。

3. 执行
   - {{描述真正会改变模型行为的操作}}。
   - 完成条件：{{所有目标项均已处理，没有遗漏项}}。

4. 验证
   - 运行 {{静态检查和真实场景验证}}。
   - 完成条件：{{结果可由命令、文件或实际业务结果证明}}。

## Delivery

- 最终输出包含 {{内容}}。
- 使用 {{格式}}。
- 分别说明已验证内容、未验证内容，以及无法完成的内容和原因。

## Guardrails

- 保护 {{用户数据、权限、稳定接口或业务边界}}。
- 遇到 {{需要额外授权或存在破坏性影响的情况}} 时停止并请求确认。
- 使用正向目标描述一般行为，只对安全红线使用强制禁止规则。

## References

- 遇到 {{分支 A}} 时，完整读取 `workflows/{{a}}.md`。
- 需要 {{输出范式}} 时，读取 `examples/{{a}}.example.md`。
- 完成实现后，执行 `scripts/{{validator}}.py`。
