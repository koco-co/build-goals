interface:
  display_name: "Build AGENTS.md"
  short_description: "创建或重构项目 Agent 指令"
  default_prompt: "使用 $build-agents-md 核查项目事实，并按我的明确请求创建、重构或审查 AGENTS.md 与对应的 CLAUDE.md；仅在关键范围仍未确定时提问。"

policy:
  allow_implicit_invocation: true
