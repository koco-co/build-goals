# 功能域实施任务：{{功能域名称}}

- 文档状态：{{已确认}}
- 更新时间：{{YYYY-MM-DD}}
- 功能域 ID：{{domain-id}}

> 模板占位以 `{{…}}` 标记，交付时全部替换为真实值；「文档状态」脚本只接受「已确认」。「集成状态」交付前必须改为「已集成」，否则 delivery 阶段校验拒绝。

## 功能域目标

{{本域交付目标、包含范围和明确排除。}}

## 输入与依赖

{{当前域需求、行为样例、直接依赖契约、共同基线和只读文件。}}

## 任务列表

### TASK-001 {{任务名称}}

- 状态：待开始
- 需求/验收/Finding：{{F/AC/SAMPLE/AUD；路线 1–3 必须包含受影响行为样例 ID}}
- 目标：{{可独立验收的外部结果}}
- 任务类型：{{行为代码|Bug|文档|配置|CI|迁移|生成文件|其他}}
- 首个验证证据：{{失败测试、现状行为测试、dry-run、lint、配置解析或其他适合任务类型的先验证据}}
- 正常测试数据：{{可重复、隔离、可清理的数据}}
- 验证命令：{{命令}}
- Worktree：{{路径或 N/A（串行执行）}}
- 集成状态：{{尚未集成}}
- 提交边界：{{允许文件和必要测试}}
- Commit：待生成
- 回滚：{{安全回滚方式}}
- 完成条件：{{唯一、可检查结果}}

## 功能域验证

{{与本域任务和验收直接对应的验证方式及未验证项。}}

## 集成与回滚

{{依赖提交、集成顺序、受影响检查、worktree 清理与恢复。}}
